// Layer 2 / Plan H1 thresholds.
//
// These mirror values defined in cleo/discovery_v2/constants.py. Keep in sync if
// the backend tunings change. Eventually the backend should expose these via
// the API so the frontend doesn't duplicate them — but that's a follow-up.
export const UNIQUELY_TENANTED_DOMINANCE_THRESHOLD = 0.6;
