# Cleo Turbo — backend (FastAPI) runtime dependencies.
# Generated by scanning imports across cleo/. Install into a project venv:
#   python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt

# --- Web server (startup-critical) ---
fastapi
uvicorn[standard]
pydantic
python-dotenv

# --- Imported at module load by cleo/ packages the app uses ---
wordfreq
cleanco

# --- Lazily imported at runtime (AI route, enrichment, scrapers-as-routes) ---
httpx
requests
anthropic
beautifulsoup4

# NOTE: The data pipeline engines (engines/rt, engines/gw scrapers) additionally
# need: playwright (run `playwright install chromium` after pip install).
# These are NOT required just to run the web app, so they're left out here.
