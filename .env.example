# Cleo Turbo local environment — copy to `.env` and fill in values.
# Anything in .env is loaded automatically by the backend at startup.

# Anthropic API key — used by /api/ai (Ask Claude) and AI HQ enrichment
ANTHROPIC_API_KEY=

# Mapbox token — if you set a different one for local than the bundled key
# MAPBOX_TOKEN=
